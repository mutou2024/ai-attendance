Backend (Spring Boot + SQLite)

Build & run locally:

mvn package
java -jar target/ai-attendance-backend-0.0.1-SNAPSHOT.jar

During first startup the DB schema is created in `./data/app.db` via `schema.sql`.
